AI = {
  {% for nation in nations %}
	c:{{ nation.id }} = {
		set_strategy = {{ strategies.administrative_choices | random_item }}
		set_strategy = {{ strategies.diplomatic_choices | random_item }}
		set_strategy = {{ strategies.politic_choices | random_item }}
	} {% endfor %}
}