BUILDINGS = {
  {% for nation in nations %}
	s:STATE_{{ nation.id }} = {
		region_state:{{ nation.id }} = {
			create_building={
				building="building_generation_ship"
				level = 1
				activate_production_methods={ "pm_generation_ship" }
			}
		}
	}
{% endfor %}
}